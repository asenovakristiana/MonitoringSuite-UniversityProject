The project is designed for and runs on Docker Desktop. It creates Nginx and Apache web servers, monitors their metrics, and stores them in a CSV file.

For training purposes only. Do not use in a production environment!
